# Agent Instructions

本文件适用于整个仓库。更深目录中的 `AGENTS.md` 可以为其子目录补充规则；发生冲突时，以更接近目标文件的规则为准。

## 开始工作前

在修改代码或项目配置前，依次阅读：

1. `README.md`
2. `docs/ARCHITECTURE.md`
3. `docs/DECISIONS.md`
4. `docs/CURRENT_STATE.md`

随后检查 `git status`、最近的 Git 历史和与任务直接相关的代码。不要仅根据历史聊天、旧计划或文档标题推断当前实现。

## Source of Truth

- 当前 Git 仓库中的代码和配置代表真实实现。
- `docs/ARCHITECTURE.md` 只描述当前有效架构；尚未实现的内容必须明确标记为目标或计划。
- 历史聊天用于理解需求来源和设计意图，不覆盖当前代码。
- 如果代码、文档和历史设计不一致，先以代码为准，并在工作结果中说明差异。

## 实现原则

- 优先做小而明确、容易验证的改动。
- 保持现有 API、命名、目录结构和代码风格，除非任务明确要求改变。
- 优先使用 Apple 平台能力和项目已有依赖；新增生产依赖前先征得确认。
- 不绕过身份认证、输入校验、系统权限或安全边界。
- Gotify Token 等敏感信息必须使用 Keychain 或安全的本地配置，不得写入源码、日志、测试快照或 Git。
- 不为通过检查而删除、跳过或弱化测试。

## 文档同步规则

当改动实质影响以下任一方面时，同步更新相关文档：

- 应用架构或模块边界
- 网络请求、WebSocket 生命周期或 Gotify 协议处理
- 数据持久化、缓存或去重策略
- Keychain、通知、登录启动或系统权限行为
- 主要依赖、部署要求或用户可见行为

具体规则：

- 当前架构发生变化时，更新 `docs/ARCHITECTURE.md`。
- 新增、替换或废弃关键设计决策时，更新 `docs/DECISIONS.md`；保留旧决策并标注状态，不要直接抹掉历史。
- 完成有意义的开发任务后，更新 `docs/CURRENT_STATE.md` 的日期、完成项、进行中事项、已知问题和下一步。
- 纯格式调整、注释或不改变行为的重构不要求更新 `CURRENT_STATE.md`。

## 完成工作前

1. 查看完整 diff，确认没有无关改动、敏感信息、调试代码或生成垃圾。
2. 运行与改动最相关的测试、构建、静态检查或格式检查。
3. 确认架构、决策和当前状态文档与代码一致。
4. 报告修改内容、验证结果以及仍存在的风险或后续事项。

除非用户明确要求，否则不要提交或推送 Git 变更。

